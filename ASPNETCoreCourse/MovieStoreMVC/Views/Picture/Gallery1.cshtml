@model string[]
@{
    ViewData["Title"] = "Gallery1";
}



<h1>Picture Gallery</h1>


@foreach (string currentPicturePath in Model)
{
    <div class="col-12 col-md-4">
        <img class="img-fluid" src="~/images/@System.IO.Path.GetFileName(currentPicturePath)" />
    </div>
}
