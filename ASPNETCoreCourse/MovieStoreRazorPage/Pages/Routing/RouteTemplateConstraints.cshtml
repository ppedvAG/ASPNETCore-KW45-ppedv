@page "{id:int}/{uid:int?}"
@*
    @page "{id:int}"
    @page "{latitude:double?}"
    @page "{id:min(10000)}"
    @page "{username:alpha:minlength(5):maxlength(8)}"*@

@model MovieStoreRazorPage.Pages.Routing.RouteTemplateConstraintsModel
@{
}
