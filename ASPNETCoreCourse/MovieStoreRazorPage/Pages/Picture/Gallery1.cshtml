@page
@model MovieStoreRazorPage.Pages.Picture.Gallery1Model
@{
}

@foreach (string currentPicturePath in Model.Bilder)
{
    <div class="col-12 col-md-4">
        <img class="img-fluid" src="~/Images/@System.IO.Path.GetFileName(currentPicturePath)" />
    </div>
}

