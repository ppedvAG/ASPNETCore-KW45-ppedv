@page
@model MovieStoreRazorPage.Pages.MovieStore.JavaScriptFormularTriggerModel
@{
}


<form id="movie-title-switcher" asp-page-handler="SelectMovieTitle" method="post">

    <div class="row">
        <div class="col-md-4">
            <select id="drpEmpList" class="form-control" asp-for="@Model.SelectedId" asp-items="@(new SelectList(Model.MovieItems, "Id", "Title"))">
                <option value="">--Select--</option>
            </select>
            <input type="hidden" asp-for="@Model.SelectedId" />

        </div>
        <div class="col-md-2">
                <input type="submit" name="btnSubmit" value="Submit" class="btn btn-success" />
            </div>
    </div>

</form>  